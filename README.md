# Agente para generar reportes personalizados de "Cerámica de Altura"

Este proyecto implementa un agente para creación de un reporte personalizado del sistema de inventario de Cerámica de Altura. Utiliza Google Agent Developer Kit (ADK) con una API expuesta mediante FastAPI. El entorno de ejecución y la gestión de dependencias se realizan con uv (modernoa administrador de paquetes de python).

## 🧠 Tecnologías principales

Google ADK (Agent Developer Kit): Framework para crear agentes inteligentes y extensibles.

FastAPI: Framework web para construir APIs rápidas y tipadas.

uv: Administrador de paquetes y entorno virtual ultrarrápido para Python.

También se tiene implementado un servidor MCP usando el framework del mismo nombre de Anthropic.

## 🚀 Requisitos previos

Antes de comenzar, es importante tener instalado los siguiente:

Python 3.12 o superior

uv (instalar mediante pip install uv)
